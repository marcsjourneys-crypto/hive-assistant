# Architecture Overview

## System Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                         CHANNELS                                 │
├─────────────────┬─────────────────┬─────────────────────────────┤
│    WhatsApp     │    Telegram     │           CLI               │
│   (Baileys)     │    (grammY)     │       (readline)            │
└────────┬────────┴────────┬────────┴──────────┬──────────────────┘
         │                 │                   │
         └─────────────────┼───────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                        GATEWAY                                   │
│  src/core/gateway.ts                                            │
│                                                                  │
│  • Receives messages from all channels                          │
│  • Manages conversations in database                            │
│  • Coordinates the full request/response cycle                  │
└─────────────────────────────┬───────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                     ORCHESTRATOR                                 │
│  src/core/orchestrator.ts                          ✅ BUILT     │
│                                                                  │
│  Provider: Haiku (cloud) or Ollama (local)                      │
│                                                                  │
│  Input:                                                          │
│  • User message                                                  │
│  • Conversation history (last 5 messages)                       │
│  • Available skills (name + description only)                   │
│                                                                  │
│  Output (RoutingDecision):                                       │
│  • selectedSkill: which skill to use (or null)                  │
│  • contextSummary: compressed history if relevant               │
│  • intent: greeting | task_query | code | creative | etc        │
│  • complexity: simple | medium | complex                        │
│  • suggestedModel: haiku | sonnet | opus                        │
│  • personalityLevel: full | minimal | none                      │
│  • includeBio: boolean                                          │
│  • bioSections: which parts of profile to include               │
└─────────────────────────────┬───────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                   CONTEXT BUILDER                                │
│  src/core/context-builder.ts                       🔨 TODO      │
│                                                                  │
│  Assembles the minimal prompt based on RoutingDecision:         │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ System Prompt                                            │    │
│  │ • Soul/Personality (if personalityLevel != none)        │    │
│  │ • User Profile (if includeBio, filtered to bioSections) │    │
│  │ • Selected Skill instructions (if selectedSkill)        │    │
│  │ • Context Summary (if present)                          │    │
│  └─────────────────────────────────────────────────────────┘    │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ Recent Messages (last 3 for continuity)                 │    │
│  └─────────────────────────────────────────────────────────┘    │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ Current User Message                                    │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────┬───────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                       EXECUTOR                                   │
│  src/core/executor.ts                              🔨 TODO      │
│                                                                  │
│  • Calls Anthropic API with built context                       │
│  • Uses model from RoutingDecision (haiku/sonnet/opus)          │
│  • Streams response (optional)                                   │
│  • Calculates cost                                               │
│  • Logs usage to database                                        │
└─────────────────────────────┬───────────────────────────────────┘
                              │
                              ▼
                    Response to User
```

## Data Layer

```
┌─────────────────────────────────────────────────────────────────┐
│                       DATABASE                                   │
│  src/db/interface.ts (abstraction)                 ✅ BUILT     │
│  src/db/sqlite.ts (implementation)                 ✅ BUILT     │
│  src/db/postgres.ts (implementation)               🔨 TODO      │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │    users     │  │conversations │  │   messages   │          │
│  ├──────────────┤  ├──────────────┤  ├──────────────┤          │
│  │ id           │  │ id           │  │ id           │          │
│  │ email        │  │ user_id      │  │ conversation │          │
│  │ config       │  │ title        │  │ role         │          │
│  │ created_at   │  │ summary      │  │ content      │          │
│  │ updated_at   │  │ created_at   │  │ created_at   │          │
│  └──────────────┘  │ updated_at   │  └──────────────┘          │
│                    └──────────────┘                              │
│  ┌──────────────┐  ┌──────────────┐                              │
│  │    skills    │  │  usage_log   │                              │
│  ├──────────────┤  ├──────────────┤                              │
│  │ id           │  │ id           │                              │
│  │ owner_id     │  │ user_id      │                              │
│  │ name         │  │ model        │                              │
│  │ description  │  │ tokens_in    │                              │
│  │ content      │  │ tokens_out   │                              │
│  │ is_shared    │  │ cost_cents   │                              │
│  └──────────────┘  │ created_at   │                              │
│                    └──────────────┘                              │
└─────────────────────────────────────────────────────────────────┘
```

## File System

```
~/.hive/
├── config.json              # Main configuration
├── data.db                  # SQLite database
├── soul.md                  # Personality definition
├── user.md                  # User profile
├── profile-updates.json     # Auto-detected profile updates
├── workspaces/
│   └── default/
│       ├── inbox/           # Files received from user
│       ├── outbox/          # Files to send to user
│       └── skills/          # User's custom skills
│           └── morning-briefing/
│               └── SKILL.md
├── skills/                  # Shared/installed skills
├── logs/
│   └── 2026-01-30.log
└── credentials/
    ├── whatsapp/            # Baileys session data
    └── telegram/            # Bot token (encrypted)
```

## Token Savings

### Traditional Approach (Hivebot-style)
```
Every request:
┌────────────────────────────┬─────────┐
│ Full system prompt         │  2,000  │
│ All skills metadata        │  3,000  │
│ Full conversation history  │  5,000  │
│ User message               │     50  │
├────────────────────────────┼─────────┤
│ TOTAL                      │ 10,050  │
└────────────────────────────┴─────────┘
```

### Our Approach
```
Simple greeting:
┌────────────────────────────┬─────────┐
│ Orchestrator call (Haiku)  │    200  │
│ Minimal personality        │    100  │
│ User message               │     50  │
├────────────────────────────┼─────────┤
│ TOTAL                      │    350  │ (96% savings!)
└────────────────────────────┴─────────┘

Task with skill:
┌────────────────────────────┬─────────┐
│ Orchestrator call (Haiku)  │    300  │
│ Minimal personality        │    100  │
│ Relevant profile section   │    200  │
│ Selected skill             │    500  │
│ Context summary            │    200  │
│ Recent messages (3)        │    300  │
│ User message               │     50  │
├────────────────────────────┼─────────┤
│ TOTAL                      │  1,650  │ (84% savings!)
└────────────────────────────┴─────────┘
```

## Model Selection

```
┌─────────────────┬──────────┬─────────────────────────────────┐
│ Intent          │ Model    │ Why                             │
├─────────────────┼──────────┼─────────────────────────────────┤
│ greeting        │ Haiku    │ Simple response needed          │
│ conversation    │ Haiku    │ Casual chat, no complexity      │
│ task_query      │ Haiku    │ Skill handles the work          │
│ briefing        │ Sonnet   │ Needs synthesis                 │
│ code            │ Sonnet   │ Reasoning required              │
│ analysis        │ Sonnet   │ Complex understanding           │
│ creative        │ Opus     │ Best quality needed             │
│ complex         │ Opus     │ When orchestrator says so       │
└─────────────────┴──────────┴─────────────────────────────────┘
```
